# dailycards

## Tasks

### dev

```bash
docker compose down
sqlc generate
docker compose up --build -d
npm run build --prefix web/frontend
npm run dev --prefix web/frontend
```

### logs

```bash
docker compose logs
```